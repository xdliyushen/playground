import React from 'react';
import './index.less';

const Pixireact = ({ className = '' }) => {
    return (
        <div className={`m-pixireact ${className}`}></div>
    );
}

export default Pixireact;
