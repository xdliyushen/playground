import React from 'react';
import './index.less';

const ReactTransitionGroup = ({ className = '' }) => (
    <div className={`m-react-transition-group ${className}`} />
);

export default ReactTransitionGroup;
